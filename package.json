{
  "name": "template",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format",
    "type:check": "turbo type:check",
    "prepare": "husky",
    "reinstall": "./scripts/reinstall"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "turbo": "^2.0.6",
    "@template/typescript-config": "workspace:*"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=18"
  }
}
