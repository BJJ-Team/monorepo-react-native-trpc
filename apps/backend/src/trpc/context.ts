// import { PrismaClient } from "@prisma/client";

export interface Context {
  // repository: {};
}

// const prismaClient = new PrismaClient();

// const repositories = {};

export const initRuntimeContext = async () => ({
  // repository: repositories,
});
